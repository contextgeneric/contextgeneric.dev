//! Code from the pages under `docs/concepts/`, one module per page.
//!
//! Three of the eighteen pages in that section are written, and two of those three show code.
//! *How much CGP to use* (`modularity-hierarchy.md`) shows none — it is a decision guide built from
//! prose and tables — so it has no module here, and neither does the section index.

pub mod coherence;
pub mod consumer_and_provider_traits;
