//! Code from the pages under `docs/comparisons/`, one module per page.
//!
//! Every page in that section shows CGP code beside code in another language. Only the CGP side is
//! checked here; the other side was compiled against the toolchain each page names in its Sources.
//! The section index shows no code, so it has no module.

pub mod algebraic_effects;
pub mod capabilities;
pub mod dependency_injection;
pub mod dynamic_dispatch;
pub mod implicit_parameters;
pub mod ml_modules;
pub mod policy_based_design;
pub mod reflection;
pub mod row_polymorphism;
pub mod rust_language_proposals;
pub mod type_classes;
